// Write a Program to find the area of a circle.

#include <iostream>
using namespace std;

main()
{
    int x, y;
    cout << "enter the value of x ";
    cin >> x;
    cout << "enter the value of y";
    cin >> y;
    cout << (2 * x) + (2 * x * y) + (2 * y);
}